version: "2"
services:
  cihm-metadatabus:
    image: cihm-metadatabus
    networks:
      - access-platform
    command: ["echo", "'unsure'"]
networks:
  access-platform:
    external:
      name: access-platform